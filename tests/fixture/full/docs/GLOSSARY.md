# Glossary

## Ledger
- **Entry** — one dated amount with a category; never deleted, only reversed (`docs/design/product-design.md §4.2`).
- **Ledger** — the JSON file holding all entries.

## Sync
- **Push** — sending local entries to the sync service (`docs/design/sync/sync-design.md §1`).
