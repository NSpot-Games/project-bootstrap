# Workflow

## 1. Lifecycle
Ground → Brainstorm → Plan → Execute → Close, one plan per feature under `docs/plans/`.

## 2. Session start
Read `docs/CURRENT.md`, then the plan being resumed or claimed.

## 3. Claiming
Set the plan `in progress` and stamp a session line. The first claim in a milestone sets that milestone `in progress`.
