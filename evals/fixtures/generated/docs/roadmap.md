# Roadmap
**No checkboxes here.** Scope and progress live in `docs/milestones/`. Edit at phase boundaries and milestone reviews.

## Principles
- Every milestone ends in something runnable or measurable.
- The current and next milestone are `planned`; everything beyond is `sketch`.
- The golden example is a test, not a document.

## P1 — First release
**Status:** active
**Exit:** the golden example builds to its expected index and `fieldnote build` handles 500 notes in under five seconds; see `docs/design/fieldnote-design.md §5`.

### M0 — Fold the golden-example gaps
Goal: every gap the golden example surfaced in the note model is closed. Features: `docs/milestones/M0.md`

### M1 — First CLI release
Goal: `fieldnote index` and `fieldnote build` work on the golden example end to end. Features: `docs/milestones/M1.md`

### M2 — Tag pages and search
**Status:** sketch
Goal: a page per tag and a client-side search box. Exit: TBD.

## P2 — Beyond one machine
**Status:** sketch
**Exit:** TBD

### M3 — Publish to a remote
Goal: TBD.

## Explicitly deferred
- Editing notes in a browser.
- Sync between machines.

## Open decisions with a deadline
See `OPEN-QUESTIONS.md`.
