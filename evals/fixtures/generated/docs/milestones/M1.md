# M1 — First CLI release
**Status:** planned
**Goal:** `fieldnote index` and `fieldnote build` work on the golden example end to end.
**Exit criteria:** `fieldnote build fixtures/golden/basic --out build/site` renders a site whose `index.json` equals the expected index.
**Evidence of exit:** `docs/evidence/M1-exit.md`
**Depends on:** M0

## Features
- [ ] M1-01 — `fieldnote index` command
- [ ] M1-02 — `fieldnote build` renders place, tag and index pages
- [ ] M1-03 — Error model for malformed frontmatter

## Notes
